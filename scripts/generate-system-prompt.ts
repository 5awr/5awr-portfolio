import { portfolioLibraryNode } from "../lib/library.node";

/* ============================================================
   npm run genprompt
   ------------------------------------------------------------
   デザインシステム（Library）から、LLM用システムプロンプトを生成して標準出力へ。
   "デザインシステムの運用 = AIの精度管理" を確認・レビューするための補助ツール。
   CIに組み込めば、コンポーネント追加時にプロンプト差分をレビューできる。
   ============================================================ */

const prompt = portfolioLibraryNode.prompt();
process.stdout.write(prompt + "\n");
